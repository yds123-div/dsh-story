{
  "name": "dsh-story",
  "private": true,
  "version": "0.1.0",
  "description": "oh-story 产品平台 monorepo",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:web": "pnpm --filter @dsh-story/web dev",
    "dev:api": "pnpm --filter @dsh-story/api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
